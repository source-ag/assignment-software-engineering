# Data

The `may.zip` file contains 14 days of weather data from the meteo sensors of one greenhouse 
(for the month of may).
Each folder within the `may.zip` file contains one day of measurements where each file contains 
measurements at a specific moment in time. One such file is generated each 5 minutes.